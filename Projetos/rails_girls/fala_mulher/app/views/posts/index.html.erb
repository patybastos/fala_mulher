<%= link_to 'New', new_post_path %>
<h1>Listing posts</h1>

<table>
 <tr>
   <th>Nome (opicional)</th>
   <th>Como podemos contactar você?</th>
   <th>Qual tipo de abuso você sofreu?</th>
   <th>O que aconteceu com você?</th>
 </tr>

 <% @posts.each do |post| %>
   <tr>
     <td><%= post.name %></td>
     <td><%= post.contact %></td>
     <td><%= post.kind %></td>
     <td><%= post.denouncement %></td>
     <td><%= link_to 'Show', post_path(post) %></td>
   </tr>
 <% end %>
</table>
